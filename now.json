{
  "version": 2,
  "name": "reddit-clone",
  "builds": [
    { "src": "client/package.json", "use": "@now/static-build" }
  ],
  "routes": [
    { "src": "/static/(.*)", "dest": "/client/static/$1" },
    { "src": "/.*", "dest": "/client" }
  ]
}
