import styled from 'styled-components/macro';

const CommentDetailAuthor = styled.span`
  margin-right: 4px;
  font-weight: 500;
  color: ${props => props.theme.normalText};
`;

export default CommentDetailAuthor;
