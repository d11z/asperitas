import styled from 'styled-components/macro';

const PostContentDetailAuthor = styled.span`
  font-weight: 500;
  color: ${props => props.theme.normalText};
`;

export default PostContentDetailAuthor;
